<%= form_with(model: autor, local: true) do |form| %>
  <% if autor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(autor.errors.count, "error") %> prohibited this autor from being saved:</h2>

      <ul>
      <% autor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :jmeno %>
    <%= form.text_field :jmeno, id: :autor_jmeno %>
  </div>

  <div class="field">
    <%= form.label :prijmeni %>
    <%= form.text_field :prijmeni, id: :autor_prijmeni %>
  </div>

  <div class="field">
    <%= form.label :narodnost %>
    <%= form.text_field :narodnost, id: :autor_narodnost %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
